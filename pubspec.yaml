name: fit_converter
description: A Dart package for converting between FIT, GPX, and TCX formats, built on top of the fit_sdk.
version: 0.1.0
repository: https://github.com/yourusername/fit_converter

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  fit_sdk: ^0.1.0
  xml: ^6.3.0
  path: ^1.8.3

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
